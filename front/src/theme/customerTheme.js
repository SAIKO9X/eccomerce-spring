import { createTheme } from "@mui/material";

export const customerTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#000",
    },
    secondary: {
      main: "#fff",
    },
  },
});
